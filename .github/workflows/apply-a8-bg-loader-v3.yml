name: Apply A8 BG Loader V3

on:
  push:
    branches:
      - dev
    paths:
      - .github/workflows/apply-a8-bg-loader-v3.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 1
      - name: Refresh continuity loader
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p = Path('tk2/a8-pdf-report.js')
          t = p.read_text(encoding='utf-8')
          old = 'battle-continuity.js?v=95a3b280'
          new = 'battle-continuity.js?v=31a9d198'
          if old not in t:
              raise SystemExit('old loader marker missing')
          p.write_text(t.replace(old, new, 1), encoding='utf-8')
          PY
          node --check tk2/a8-pdf-report.js
      - name: Commit loader
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tk2/a8-pdf-report.js
          git commit -m "fix: refresh A8 battle backdrop loader v3"
          git push origin HEAD:dev
